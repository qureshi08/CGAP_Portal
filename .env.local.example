# Create a new, separate Supabase project for CGAP Portal (do not reuse the
# Recruitment Portal's project) — see TECH_STACK.md.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# SMTP for templated emails (Nodemailer) — same pattern as the Recruitment Portal.
EMAIL_USER=
EMAIL_PASS=

NEXT_PUBLIC_APP_URL=http://localhost:3000
